import styled from 'styled-components'

// ContainerChild 스타일 컴포넌트
export const ContainerChild = styled('div')`
  border: 2px dashed #333;
  margin: 10px;
  padding: 10px;
`

ContainerChild.displayName = 'ContainerChild'
